{
  "name": "aperture-server",
  "version": "1.0.0",
  "description": "Aperture Server is a websocket-based implementation that allows a *nix or windows device to share its terminal with a web-based interface for easy access of the device, remotely. The server implements socket.io on the backend as a means of channeling the communication between your frontend and the device you want to connect to.",
  "main": "server/app.js",
  "scripts": {
    "start": "node server/app.js",
    "standard": "npx standard --fix"
  },
  "repository": {
    "type": "git",
    "url": "."
  },
  "engines": {
    "node": "10.x.x"
  },
  "keywords": [
    "aperture",
    "socket.io",
    "websocket",
    "server",
    "cli"
  ],
  "author": "Jordan Violet",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "ioredis": "^3.1.4",
    "moment": "^2.24.0",
    "socket.io": "2.0.4",
    "socket.io-redis": "^5.1.0",
    "socketio-auth": "^0.1.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "standard": "*"
  }
}
